{% include 'admin/head.html.twig' %}
{% include 'admin/menu.html.twig' %}
<div id="wrapper">
    {% include 'admin/sales/sidebar.html.twig' %}
    <div id="content-wrapper">
        <div class="container-fluid">
            <!-- Breadcrumbs-->
            <ol class="breadcrumb">
                <li class="breadcrumb-item">
                    <a href="#">My Deals</a>
                </li>
                <li class="breadcrumb-item active">Details</li>
            </ol>
            <!-- DataTables Example -->
            <div class="card mb-3">
                <div class="card-header">
                    <i class="fas fa-table"></i>
                    Deals  [ You can add <b>{{ limits.deals_limit - header_totals.deals }}</b> more Deals as per your current limit. But you can increase it by using <a href="#" data-toggle="modal" data-target="#referral_help_modal">Referral Code</a>. ]
                    <span id="response_display" class="alert"></span>
                </div>
                <ul id="tabs_add_leads" class="nav nav-tabs nav-tabs-responsive">
                    {{ tabs|raw }}
                </ul>
            </div>
            <br clear="all">
            <div class="card-body">
                <div class="table-responsive">
                    {{ containers|raw }}
                </div>
            </div>
            <div class="card-footer small text-muted">Updated
                {{ "now"|date("F jS Y \\a\\t g:ia") }}</div>
        </div>
        <!-- google adsence -->
        <div id="advt" class="card-body">
            
        </div>
        <!-- google adsence -->
        <!-- /.container-fluid -->
        {% include 'admin/copyright.html.twig' %}
    </div>
    <!-- /.content-wrapper -->
</div>
<!-- /#wrapper -->
<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
</a>
<!-- Deal won Modal pop up when select any deal to move as won -->
<div class="modal fade" id="dealwonModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Deal Won
                    <i class="fas fa-trophy"></i>
                </h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">Congratulation! what was the main steps behind won this deal? any feed back.</div>
            <div class="container_tabs_add_forms">
                <form id="frm_deal_won" action="/sales/dealwon" method="post" class="simple-form">
                    <div id="deal_won_result" name="deal_won_result" class="alert" role="alert"></div>
                    <input type="hidden" id="own_deal_stage" name="own_deal_stage" value="">
                    <input type="hidden" id="own_deal_id" name="own_deal_id" value="">
                    <div class="form-row">
                        <div class="form-group">
                            <div class="col-md-12">
                                <textarea name="deal_won_feedback" id="deal_won_feedback" rows="3" cols="50"></textarea>
                            </div>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <div class="col-md-12">
                                <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button> <input type="submit" value="Submit" class="btn btn-secondary">
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<!-- Deal lost Modal pop up when select any deal move as won -->
<div class="modal fade" id="deallostModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Deal Lost
                    <i class="fas fa-balance-scale-left"></i>
                </h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">Try hard be prepared! what was the cause lost this deal? any feed back.</div>
            <div class="container_tabs_add_forms">
                <form id="frm_deal_lost" action="/sales/deallost" method="post" class="simple-form">
                    <div id="deal_lost_result" name="deal_lost_result" class="alert" role="alert"></div>
                    <input type="hidden" id="lost_deal_id" name="lost_deal_id" value="">
                    <div class="form-row">
                        <div class="form-group">
                            <div class="col-md-12">
                                <textarea name="deal_lost_feedback" id="deal_lost_feedback" rows="3" cols="50"></textarea>
                            </div>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <div class="col-md-12">
                                <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button> <input type="submit" value="Submit" class="btn btn-secondary">
                            </div>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Referral Modal pop up when select any deal move as won -->
<div class="modal fade" id="referral_help_modal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Referral system
                    <i class="fa fa-user-plus" aria-hidden="true"></i>
                </h5>
                <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">×</span>
                </button>
            </div>
            <div class="modal-body">You just share your Referral Code to your friends. Each person register to EsyCRM both of you will get additional number of Deals and Contacts.<br>
            Referral Code: Click on My Profile & Admin Panel, you will get your unique Referral Code. Which you need to share to your friends which they will use time of Sign Up for EsyCRM. Have a nice day.</div>            
        </div>
    </div>
</div>
{% include 'admin/footer.html.twig' %}